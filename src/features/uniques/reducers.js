import uniquesData from '../../static/uniques.json';

const INITIAL_STATE = { uniquesData };

const uniques = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default uniques;
