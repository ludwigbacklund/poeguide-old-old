import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  color: chartreuse;
`;

const App = () => {
  return <Wrapper>All good!</Wrapper>;
};

export default App;
